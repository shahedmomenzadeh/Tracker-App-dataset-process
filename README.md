Create a folder named: Pupil Labels in the root directory and place the pupil labels labels only (in the .txt format) in that folder.

Then just open the pipeline.ipynb and run all the cells sequentially.